# RAG API base URL — the real port comes from the uvicorn output
RAG_API_URL=http://localhost:8000
# Must match the backend's RAG_API_KEY. Server-side only — never NEXT_PUBLIC_.
RAG_API_KEY=change-me
